<h1 class="text-2xl font-bold mb-1">Home</h1>
<p class="text-gray-600 mb-4">
  Welcome, <strong><%= SessionsController::PERSONAS.dig(current_user, :name) %></strong>.
</p>
<p class="text-sm text-gray-500 leading-relaxed">
  This demo shows server-driven tab bar navigation via the Hotwire Native bridge.
  The tab configuration is generated by Rails on every page load and sent to the
  native app through a Stimulus bridge controller. Use the feature toggles above
  to change the tab layout in real time.
</p>
